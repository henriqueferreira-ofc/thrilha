# Netlify redirects
# Cache assets with long-term caching
/assets/* 200

# API redirects (if needed in future)
# /api/* 200

# SPA catch-all - redirect all other routes to index.html
/* /index.html 200

